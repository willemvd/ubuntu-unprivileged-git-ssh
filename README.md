# ubuntu-unprivileged-git-ssh

Run Ubuntu with SSH and Git support as unprivileged user so it can be used in Docker environments where only non-root images are allowed, like OpenShift Dedicated/Online
